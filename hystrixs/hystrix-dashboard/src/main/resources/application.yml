server:
  port: 8030

spring:
  datasource:
    #    schema: classpath:schema.sql
    #    data: classpath:data.sql
    url: jdbc:h2:mem:test

  jpa:
    show-sql: true
  h2:
    console:
      enabled: true